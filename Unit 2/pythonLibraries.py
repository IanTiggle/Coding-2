# Library = Place that stores a lot of books/ information

# Python Libraries = files/folders that contain functions and data that we can use quickly and easily. These are code instructions that
# were already written for us. We just need to call it.

# We use the IMPORT keyword to bring in libraries/ modules into our code.

# Built in modules - a object that is already built in to the programming language. 
import random

number = random.randrange(1,10)

# locan modules - boject that stored inside of a file in our system.
import example



example.Pets("Cloud", "Cat", 2, "White", 10)

pet1 = example.Pets("Cloud", "Cat", 2, "White", 10)
print(pet1.name)
